main.remove();

const newHeader = document.createElement("h1");
//document.body.append(newHeader); adds the new element to the body

newHeader.id = "victory";

newHeader.textContent = "William is the champion";